# Supabase Configuration
EXPO_PUBLIC_SUPABASE_URL=your_supabase_url
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# OpenAI Configuration  
EXPO_PUBLIC_OPENAI_API_KEY=your_openai_api_key

# Google OAuth Configuration (required for Google Sign-In via Supabase)
# Only the Web Client ID is needed for Supabase OAuth
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=your_google_web_client_id

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual credentials
# 3. For Google OAuth setup, follow the README instructions for Google Cloud Console configuration
# 4. Make sure .env is listed in .gitignore to keep your credentials secure 